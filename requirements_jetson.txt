# WALL-E Jetson Orin Nano (8GB) — minimal requirements
# PyTorch is NOT listed here — Jetson has its own CUDA-aware build pre-installed.
# Install with: pip install -r requirements_jetson.txt

# --- Core / LLM ---
openai>=1.0
pydantic>=2.0
requests

# --- Memory & Embeddings ---
sentence-transformers
faiss-cpu
numpy

# --- Serial / Arduino ---
pyserial

# --- STT ---
moonshine-voice

# --- TTS (Mimic3 HTTP client) ---
sounddevice
scipy

# --- Vision (base) ---
opencv-python
pillow

# --- Web API ---
flask

# --- Optional: Coral TPU (uncomment if using Coral) ---
# tflite-runtime
# pycoral

# --- Optional: CPU vision fallback (uncomment if NOT using Coral) ---
# ultralytics
# onnxruntime
